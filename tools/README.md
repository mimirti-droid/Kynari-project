# tools/

Herramientas auxiliares y prototipos.

Pendiente de subir:

- `kynari-home.html`: prototipo de la homepage (referencia visual).
- `kynari-generator.html`: Kynari Article Generator. App HTML standalone que llama a la API de Anthropic desde el navegador; flujo de 3 pasos (tema → 3 ángulos → artículo), editor inline, panel AI Rewrite y panel SEO/Meta.
